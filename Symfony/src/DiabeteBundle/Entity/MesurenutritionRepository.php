<?php

namespace DiabeteBundle\Entity;

use Doctrine\ORM\EntityRepository;

/**
 * MesurenutritionRepository
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class MesurenutritionRepository extends EntityRepository
{
	public function getMesureNutrition(Subscriber $subscriber, $nbDays) {
		$now = new \DateTime();
		$nbDaysAgo = new \DateTime("-{$nbDays} days");

		$qb = $this->createQueryBuilder('btd')
						->where('btd.idAbonne = :subscriber')
						->andWhere('btd.heure BETWEEN :nbDaysAgo AND :now')
						->setParameter('subscriber', $subscriber)
						->setParameter('nbDaysAgo', $nbDaysAgo)
						->setParameter('now', $now)
						->orderBy('btd.heure','desc');
		return $qb->getQuery()->getResult();
	}
}
